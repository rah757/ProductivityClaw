[pytest]
markers =
    llm: tests that require MLX server at localhost:8000 (deselect with: -m "not llm")
    deepeval: LLM-as-judge quality metrics (deselect with: -m "not deepeval")
    flaky: tests that may fail due to non-deterministic LLM responses
